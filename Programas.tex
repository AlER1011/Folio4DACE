\documentclass{FolioDACE}
%This is the header of the file, where the libraries and configurations are defined.
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{pdfpages} %This is the library that allow importing external PDFs.
%\usepackage[right=0.35in]{geometry}% We change the right margin, so that the header is correctly placed.
%\newcommand\fourdigits[1]{% This command is used to change the formating of a number to have 4 digits.
%   \ifnum#1<10 000#1\else \ifnum#1<100 00#1\else \ifnum#1<1000 0#1 \else #1 \fi\fi\fi
%}
%\usepackage{fancyhdr}% http://ctan.org/pkg/fancyhdr
%\fancypagestyle{VerticalNumbering}{%
%  \fancyhf{}% Clear header/footer
%  \fancyhead{% Header
%  \makebox[\textwidth][r]{% Right
%  \rotatebox{-90}{\fourdigits{\thepage}}}}}% Rotates the header box containing the page number and increases it's digit count.
%\renewcommand{\headrulewidth}{0pt}% No header rule
%\renewcommand{\footrulewidth}{0pt}% No footer rule
  
  
\begin{document}

\pagenumbering{arabic}%We begin the page count for the PENSUM.
\includepdf[pages={1-5},pagecommand={\thispagestyle{VerticalNumbering}}]{PENSUM.pdf}
\pagenumbering{arabic}%We begin the new page count for the Course Programs.
\includepdf[pages={-},pagecommand={\thispagestyle{VerticalNumbering}}]{Program-1.pdf}
\includepdf[pages={-},pagecommand={\thispagestyle{VerticalNumbering}}]{Program-2.pdf}

\end{document}
